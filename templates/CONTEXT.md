# <Context Name>

<One or two sentences: what this context is and why it exists.>

## Language

<Each term specific to THIS project's domain — not general programming concepts.
Pick the canonical word; list the synonyms it replaces under _Avoid_. One or two
sentences defining what it IS, not what it does. Group under subheadings when
clusters emerge; a flat list is fine for a small domain. Delete the examples below
and replace them with real terms as they are resolved.>

**<Term>**:
<What it is.>
_Avoid_: <synonym>, <synonym>

**<Term>**:
<What it is.>
_Avoid_: <synonym>

<!--
  Format reference: the vendored domain-modeling skill at
  .claude/skills/engineering/domain-modeling/CONTEXT-FORMAT.md.
  Rules, in brief:
  - Be opinionated — pick one word per concept, list the rest under _Avoid_.
  - Keep definitions to one or two sentences; define what it IS, not what it does.
  - Only project-specific concepts belong here, never general programming ones.
  - This file is a glossary only — no implementation details, no specs, no decisions.
    Decisions that are hard to reverse and surprising without context go in docs/adr/.
-->
